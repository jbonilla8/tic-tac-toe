import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        alignItems: 'center',
        paddingTop: 120
    },
    logo: {
        height: 150,
        maxWidth: '80%',
        resizeMode: 'contain',
    },
    buttons: {
        marginTop: 40,
        marginHorizontal: 20
    },
    button: {
        marginBottom: 20
    }
});

export default styles;